<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/home"
        android:icon="@drawable/ic_home"
        android:title="Bài Đăng"
        app:cnb_backgroundColor="@color/colorText"
        app:cnb_iconColor="#673AB7"
        app:cnb_textColor="#ffffff" />

    <item
        android:id="@+id/cart"
        android:icon="@drawable/ic_status"
        android:title="Tình Trạng"
        app:cnb_backgroundColor="@color/colorText"
        app:cnb_iconColor="#ffffff"
        app:cnb_textColor="#ffffff" />

    <item
        android:id="@+id/profile"
        android:icon="@drawable/ic_map"
        android:title="Tuyến Đường"
        app:cnb_backgroundColor="@color/colorText"
        app:cnb_iconColor="#ffffff"
        app:cnb_textColor="#ffffff" />

    <item
        android:id="@+id/canhan"
        android:enabled="true"
        android:icon="@drawable/ic_profile"
        android:title="Cá Nhân"
     />

</menu>